# Reference

The rendered Sila execution specification is generated by `docc`
from the Python source under `src/sila/`.

At deploy time, this placeholder is replaced by the `docc` output. To
build the reference locally, run:

```bash
just docs-spec
```
